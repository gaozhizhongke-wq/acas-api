name: Test Runner

on:
  push:
    branches: [main]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from GitHub Actions"
      - run: echo "Testing basic runner"
      - run: date
