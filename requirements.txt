# ACAS v2 - Enterprise Dependencies (Optimized)
# Python 3.11+

# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.30
psycopg[binary]==3.1.19      # Sync-only; NOT used for async engine (see asyncpg below)
asyncpg==0.29.0                # Async driver for SQLAlchemy async engine (Linux/Docker)
alembic==1.13.1
aiosqlite==0.20.0  # For testing

# Caching & Queue
redis==5.0.4
celery==5.4.0

# Security
passlib[argon2]==1.7.4
pyjwt==2.8.0
cryptography==42.0.7

# Configuration
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# ML & Data Science (lightweight)
numpy==1.26.4
pandas==2.2.2
scipy==1.13.0
statsmodels==0.14.2

# NLP - Sentiment Analysis (optional but recommended)
# ── ML dependencies (lazy-loaded; degrade gracefully when absent) ────────
# SentencePiece is required by the XLM-RoBERTa tokenizer.
# Install transformers to enable the ML sentiment model (XLM-RoBERTa).
# Without it, the system falls back to the rule-based sentiment engine.
# torch is optional — transformers runs on CPU for inference (no GPU required).
sentencepiece==0.2.0
# transformers==4.41.0  # Uncomment for ML sentiment (adds ~1 GB to image)
# torch==2.3.0          # Optional: uncomment if torch is needed for inference
# accelarate==0.30.0

# Alternative: Use ONNX Runtime for lighter deployment
# onnxruntime==1.18.0
# optimum==1.20.0

# Prophet for time series (optional)
# prophet==1.1.5

# News & Web
feedparser==6.0.11
aiohttp==3.9.5

# Observability
prometheus-client==0.20.0
structlog==24.1.0
sentry-sdk[fastapi]==1.45.1  # Error tracking — set ACAS_SENTRY_DSN to enable

# Testing
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0

# Development
black==24.4.2
ruff==0.4.5
mypy==1.10.0

# Production
gunicorn==22.0.0
